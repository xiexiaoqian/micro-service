server:
  port: 80

spring:
  application:
    name: consumer-mosoteach

info:
  app:
    name: consumer-mosoteach-hello~~~

eureka:
  instance:
    hostname: xxq.com # 主机名
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:7002/eureka/
